/* index.wxss - 情绪罗盘首页样式 */

/* 页面基础 */
.page {
  /* 升级背景渐变，使用更现代的色彩搭配 */
  background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
  min-height: 100vh;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  overflow-x: hidden;
  /* 添加背景纹理效果 */
  position: relative;
}

/* 添加背景纹理层 */
.page::before {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: 
    radial-gradient(circle at 20% 80%, rgba(255, 255, 255, 0.1) 0%, transparent 50%),
    radial-gradient(circle at 80% 20%, rgba(255, 255, 255, 0.1) 0%, transparent 50%),
    radial-gradient(circle at 40% 40%, rgba(255, 255, 255, 0.05) 0%, transparent 50%);
  pointer-events: none;
  z-index: 0;
}

/* 主容器 */
.compass-container {
  position: relative;
  min-height: 100vh;
  padding: 40rpx 30rpx;
  padding-bottom: calc(200rpx + env(safe-area-inset-bottom));
  padding-bottom: calc(200rpx + constant(safe-area-inset-bottom));
  box-sizing: border-box;
  z-index: 1;
}

/* 背景装饰 */
.bg-decoration {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  z-index: 0;
}

.floating-particle {
  position: absolute;
  width: 8rpx;
  height: 8rpx;
  /* 优化粒子效果，添加渐变色彩 */
  background: radial-gradient(circle, rgba(255, 255, 255, 0.8) 0%, rgba(255, 255, 255, 0.2) 100%);
  border-radius: 50%;
  animation: float 6s ease-in-out infinite;
  animation-delay: var(--delay);
  left: var(--x);
  top: var(--y);
  /* 添加发光效果 */
  box-shadow: 0 0 10rpx rgba(255, 255, 255, 0.3);
}

@keyframes float {
  0%, 100% { 
    transform: translateY(0px) scale(1); 
    opacity: 0.3; 
  }
  50% { 
    transform: translateY(-30rpx) scale(1.3); 
    opacity: 0.9; 
    /* 添加发光动画 */
    box-shadow: 0 0 20rpx rgba(255, 255, 255, 0.5);
  }
}

/* 用户状态区域 */
.user-status {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 60rpx;
  z-index: 1;
  position: relative;
}

/* 头像容器：严格正圆，避免按钮影响形状 */
.avatar-circle {
  position: relative;
  width: 140rpx;
  height: 140rpx;
  margin-bottom: 24rpx;
  border-radius: 50%;
  overflow: hidden;
}

.avatar-image {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  object-fit: cover;
  background: #d9d9d9; /* 灰色默认底色，若无头像时不显突兀 */
}

/* 覆盖其上的透明按钮，仅用于触发选择器 */
.avatar-overlay-btn {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  padding: 0;
  background: transparent;
  border: none;
  border-radius: 50%;
}

.avatar-overlay-btn::after {
  border: none; /* 去掉原生按钮的默认边框效果 */
}

@keyframes pulse {
  0%, 100% { 
    transform: scale(1); 
    opacity: 0.4; 
    border-color: rgba(255, 255, 255, 0.4);
  }
  50% { 
    transform: scale(1.15); 
    opacity: 0.8; 
    border-color: rgba(255, 255, 255, 0.8);
  }
}

.greeting-text {
  font-size: 36rpx;
  font-weight: 600;
  color: #fff;
  margin-bottom: 12rpx;
  /* 增强文字阴影效果 */
  text-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.3);
  letter-spacing: 1rpx;
}

.date-info {
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.9);
  letter-spacing: 1rpx;
  /* 添加背景装饰 */
  background: rgba(255, 255, 255, 0.1);
  padding: 8rpx 16rpx;
  border-radius: 20rpx;
  backdrop-filter: blur(10rpx);
}

/* 用户信息设置界面 */
.profile-setup {
  margin-top: 60rpx;
  padding: 40rpx;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 30rpx;
  backdrop-filter: blur(20rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.2);
  animation: fadeInUp 0.8s ease-out;
}

.profile-header {
  text-align: center;
  margin-bottom: 40rpx;
}

.profile-title {
  display: block;
  font-size: 36rpx;
  font-weight: 600;
  color: #fff;
  margin-bottom: 12rpx;
}

.profile-subtitle {
  display: block;
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.8);
}

.avatar-setup {
  margin-bottom: 40rpx;
}

.setup-label {
  display: block;
  font-size: 28rpx;
  color: #fff;
  margin-bottom: 20rpx;
  font-weight: 500;
}

.avatar-btn {
  position: relative;
  width: 120rpx;
  height: 120rpx;
  margin: 0 auto;
  padding: 0;
  background: transparent;
  border: none;
  border-radius: 50%;
  overflow: hidden;
}

.avatar-btn::after {
  border: none;
}

.preview-avatar {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  border: 3rpx solid rgba(255, 255, 255, 0.3);
}

.default-avatar {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  border: 3rpx solid rgba(255, 255, 255, 0.3);
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.2), rgba(255, 255, 255, 0.1));
  display: flex;
  align-items: center;
  justify-content: center;
}

.avatar-placeholder {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.7);
}

.avatar-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0;
  transition: opacity 0.3s;
}

.avatar-btn:hover .avatar-overlay {
  opacity: 1;
}

.avatar-hint {
  font-size: 20rpx;
  color: #fff;
  text-align: center;
  line-height: 1.2;
}

.nickname-setup {
  margin-bottom: 40rpx;
}

.nickname-input {
  width: 100%;
  height: 80rpx;
  background: rgba(255, 255, 255, 0.1);
  border: 1rpx solid rgba(255, 255, 255, 0.3);
  border-radius: 15rpx;
  padding: 0 20rpx;
  font-size: 28rpx;
  color: #fff;
  box-sizing: border-box;
}

.nickname-input::-webkit-input-placeholder {
  color: rgba(255, 255, 255, 0.6);
}

.complete-btn {
  width: 100%;
  height: 90rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border: none;
  border-radius: 45rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s ease;
  box-shadow: 0 8rpx 20rpx rgba(102, 126, 234, 0.3);
}

.complete-btn::after {
  border: none;
}

.complete-btn[disabled] {
  background: rgba(255, 255, 255, 0.2);
  box-shadow: none;
  opacity: 0.6;
}

.complete-btn:not([disabled]):hover {
  transform: translateY(-2rpx);
  box-shadow: 0 12rpx 30rpx rgba(102, 126, 234, 0.4);
}

.complete-text {
  font-size: 30rpx;
  font-weight: 600;
  color: #fff;
}

/* 登录容器 */
.login-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 70vh;
  text-align: center;
  z-index: 1;
  position: relative;
}

.compass-logo {
  margin-bottom: 80rpx;
  /* 添加整体动画效果 */
  animation: logoFloat 4s ease-in-out infinite;
}

/* 新增logo浮动动画 */
@keyframes logoFloat {
  0%, 100% { transform: translateY(0rpx); }
  50% { transform: translateY(-10rpx); }
}

.compass-ring {
  position: relative;
  width: 200rpx;
  height: 200rpx;
  /* 升级罗盘环效果 */
  border: 6rpx solid rgba(255, 255, 255, 0.4);
  border-radius: 50%;
  background: 
    radial-gradient(circle, rgba(255, 255, 255, 0.15) 0%, rgba(255, 255, 255, 0.05) 100%);
  backdrop-filter: blur(15rpx);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 
    0 0 0 2rpx rgba(255, 255, 255, 0.1),
    0 20rpx 40rpx rgba(0, 0, 0, 0.2),
    inset 0 0 20rpx rgba(255, 255, 255, 0.1);
  animation: compassGlow 3s ease-in-out infinite;
}

/* 新增罗盘发光动画 */
@keyframes compassGlow {
  0%, 100% { 
    box-shadow: 
      0 0 0 2rpx rgba(255, 255, 255, 0.1),
      0 20rpx 40rpx rgba(0, 0, 0, 0.2),
      inset 0 0 20rpx rgba(255, 255, 255, 0.1);
  }
  50% { 
    box-shadow: 
      0 0 0 4rpx rgba(255, 255, 255, 0.2),
      0 25rpx 50rpx rgba(0, 0, 0, 0.3),
      inset 0 0 30rpx rgba(255, 255, 255, 0.2);
  }
}

.compass-needle {
  position: absolute;
  width: 4rpx;
  height: 80rpx;
  /* 升级指针渐变效果 */
  background: linear-gradient(to top, 
    rgba(255, 255, 255, 0.3), 
    rgba(255, 255, 255, 0.9), 
    #ff6b6b);
  border-radius: 2rpx;
  left: 50%;
  bottom: 50%;
  transform-origin: bottom center;
  transform: translateX(-50%);
  animation: needleSwing 4s ease-in-out infinite;
  /* 添加指针发光效果 */
  box-shadow: 0 0 10rpx rgba(255, 107, 107, 0.5);
}

@keyframes needleSwing {
  0%, 100% { 
    transform: translateX(-50%) rotate(-8deg); 
    box-shadow: 0 0 10rpx rgba(255, 107, 107, 0.5);
  }
  50% { 
    transform: translateX(-50%) rotate(8deg); 
    box-shadow: 0 0 15rpx rgba(255, 107, 107, 0.8);
  }
}

.compass-dot {
  width: 16rpx;
  height: 16rpx;
  /* 升级中心点效果 */
  background: radial-gradient(circle, #fff 0%, rgba(255, 255, 255, 0.8) 100%);
  border-radius: 50%;
  z-index: 1;
  box-shadow: 0 0 10rpx rgba(255, 255, 255, 0.8);
}

.brand-text {
  margin-bottom: 100rpx;
  text-align: center;
  width: 100%;
}

.brand-title {
  display: block;
  font-size: 64rpx;
  font-weight: 700;
  color: #fff;
  margin-bottom: 24rpx;
  /* 增强标题效果 */
  text-shadow: 
    0 4rpx 16rpx rgba(0, 0, 0, 0.3),
    0 0 30rpx rgba(255, 255, 255, 0.2);
  letter-spacing: 4rpx;
  text-align: center;
  background: linear-gradient(135deg, #fff 0%, rgba(255, 255, 255, 0.8) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.brand-subtitle {
  display: block;
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.9);
  line-height: 1.6;
  text-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.2);
  text-align: center;
  letter-spacing: 1rpx;
  white-space: pre-line;
  max-width: 400rpx;
  margin: 0 auto;
  /* 添加背景装饰 */
  background: rgba(255, 255, 255, 0.1);
  padding: 12rpx 24rpx;
  border-radius: 25rpx;
  backdrop-filter: blur(10rpx);
}

.login-btn {
  /* 升级登录按钮效果 */
  background: linear-gradient(135deg, 
    rgba(255, 255, 255, 0.25) 0%, 
    rgba(255, 255, 255, 0.15) 100%);
  border: 2rpx solid rgba(255, 255, 255, 0.4);
  border-radius: 60rpx;
  padding: 32rpx 100rpx;
  backdrop-filter: blur(15rpx);
  transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  min-width: 320rpx;
  width: auto;
  display: flex;
  justify-content: center;
  align-items: center;
  white-space: nowrap;
  box-shadow: 
    0 8rpx 32rpx rgba(0, 0, 0, 0.15),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.2);
  position: relative;
  overflow: hidden;
}

/* 添加按钮光效 */
.login-btn::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(255, 255, 255, 0.2), 
    transparent);
  transition: left 0.6s;
}

.login-btn:hover::before,
.login-btn-hover::before {
  left: 100%;
}

.login-btn:hover,
.login-btn-hover {
  background: linear-gradient(135deg, 
    rgba(255, 255, 255, 0.35) 0%, 
    rgba(255, 255, 255, 0.25) 100%) !important;
  transform: translateY(-6rpx) scale(1.02);
  box-shadow: 
    0 16rpx 48rpx rgba(0, 0, 0, 0.25),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.3);
  border-color: rgba(255, 255, 255, 0.6);
}

.login-text {
  font-size: 30rpx;
  color: #fff;
  font-weight: 600;
  letter-spacing: 2rpx;
  text-align: center;
  white-space: nowrap;
  display: inline-block;
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.2);
}

/* ===== 新版头像昵称填写界面样式 ===== */
.profile-setup {
  background: rgba(255, 255, 255, 0.1);
  border-radius: 40rpx;
  padding: 60rpx 40rpx;
  margin-top: 40rpx;
  backdrop-filter: blur(15rpx);
  border: 2rpx solid rgba(255, 255, 255, 0.2);
}

.setup-title {
  text-align: center;
  margin-bottom: 60rpx;
}

.title-main {
  display: block;
  color: rgba(255, 255, 255, 0.95);
  font-size: 36rpx;
  font-weight: 700;
  margin-bottom: 16rpx;
}

.title-sub {
  display: block;
  color: rgba(255, 255, 255, 0.7);
  font-size: 28rpx;
  line-height: 1.4;
}

/* 头像选择器 */
.avatar-picker {
  position: relative;
  width: 160rpx;
  height: 160rpx;
  margin: 0 auto 40rpx;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.1);
  border: 4rpx solid rgba(255, 255, 255, 0.3);
  overflow: hidden;
  transition: all 0.3s ease;
}

.avatar-picker:hover {
  transform: scale(1.05);
  border-color: rgba(255, 255, 255, 0.5);
}

.picker-avatar {
  width: 100%;
  height: 100%;
  border-radius: 50%;
}

.picker-overlay {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: rgba(0, 0, 0, 0.6);
  color: white;
  padding: 8rpx;
  text-align: center;
  backdrop-filter: blur(5rpx);
}

.picker-hint {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.9);
}

/* 昵称输入框 */
.nickname-input {
  background: rgba(255, 255, 255, 0.15);
  border: 2rpx solid rgba(255, 255, 255, 0.3);
  border-radius: 25rpx;
  padding: 24rpx 30rpx;
  margin-bottom: 40rpx;
  color: rgba(255, 255, 255, 0.95);
  font-size: 32rpx;
  text-align: center;
  backdrop-filter: blur(10rpx);
  transition: all 0.3s ease;
}

.nickname-input:focus {
  border-color: rgba(255, 255, 255, 0.6);
  background: rgba(255, 255, 255, 0.2);
}

/* 完成按钮 */
.complete-btn {
  background: linear-gradient(135deg, #4CAF50 0%, #45a049 100%);
  border: none;
  border-radius: 30rpx;
  padding: 28rpx 60rpx;
  margin-top: 20rpx;
  transition: all 0.3s ease;
  box-shadow: 0 8rpx 30rpx rgba(76, 175, 80, 0.3);
}

.complete-btn.active {
  background: linear-gradient(135deg, #4CAF50 0%, #45a049 100%);
}

.complete-btn.disabled {
  background: rgba(255, 255, 255, 0.2);
  box-shadow: none;
}

.complete-btn:hover,
.complete-btn.active:hover {
  transform: translateY(-4rpx);
  box-shadow: 0 12rpx 40rpx rgba(76, 175, 80, 0.4);
}

.complete-text {
  color: white;
  font-size: 32rpx;
  font-weight: 600;
}

.complete-btn.disabled .complete-text {
  color: rgba(255, 255, 255, 0.5);
}

/* 设置按钮 */
.setup-btn {
  background: linear-gradient(135deg, 
    rgba(255, 255, 255, 0.2) 0%, 
    rgba(255, 255, 255, 0.1) 100%);
  border: 2rpx solid rgba(255, 255, 255, 0.3);
  border-radius: 30rpx;
  padding: 24rpx 48rpx;
  margin-top: 20rpx;
  transition: all 0.3s ease;
  backdrop-filter: blur(10rpx);
}

.setup-btn:hover,
.setup-btn-hover {
  background: linear-gradient(135deg, 
    rgba(255, 255, 255, 0.3) 0%, 
    rgba(255, 255, 255, 0.2) 100%);
  transform: translateY(-4rpx);
  box-shadow: 0 10rpx 30rpx rgba(0, 0, 0, 0.15);
}

.setup-text {
  color: rgba(255, 255, 255, 0.9);
  font-size: 28rpx;
  font-weight: 500;
}

/* 卡片容器 */
.card-container {
  z-index: 1;
  position: relative;
  margin-bottom: 80rpx;
  width: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
}

/* AI生成的卡片图片展示区域 */
.card-image-display {
  position: relative;
  width: 100%;
  border-radius: 32rpx;
  overflow: hidden;
  /* 升级卡片阴影效果 */
  box-shadow: 
    0 40rpx 80rpx rgba(0,0,0,0.2),
    0 0 0 1rpx rgba(255, 255, 255, 0.1);
  transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  background: none;
}

.card-image-display:active {
  transform: scale(0.97);
  box-shadow: 
    0 20rpx 40rpx rgba(0,0,0,0.3),
    0 0 0 1rpx rgba(255, 255, 255, 0.2);
}

.generated-card-image {
  width: 750rpx;
  height: 1200rpx;
  max-width: 100%;
  display: block;
}

.card-overlay {
  position: absolute;
  top: 30rpx;
  right: 30rpx;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  align-items: flex-end;
}

.ai-badge {
  /* 升级AI标识效果 */
  background: linear-gradient(135deg, rgba(0,0,0,0.9) 0%, rgba(0,0,0,0.7) 100%);
  color: white;
  padding: 12rpx 24rpx;
  border-radius: 40rpx;
  font-size: 24rpx;
  backdrop-filter: blur(20rpx);
  font-weight: 500;
  border: 1rpx solid rgba(255, 255, 255, 0.2);
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.3);
}

.interactive-hint {
  /* 升级交互提示效果 */
  background: linear-gradient(135deg, rgba(138, 43, 226, 0.95) 0%, rgba(168, 85, 247, 0.95) 100%);
  color: white;
  padding: 8rpx 20rpx;
  border-radius: 30rpx;
  font-size: 22rpx;
  backdrop-filter: blur(20rpx);
  animation: hintPulse 2s ease-in-out infinite;
  border: 1rpx solid rgba(255, 255, 255, 0.3);
  box-shadow: 0 4rpx 16rpx rgba(138, 43, 226, 0.4);
}

@keyframes hintPulse {
  0%, 100% { 
    transform: scale(1); 
    opacity: 0.9; 
    box-shadow: 0 4rpx 16rpx rgba(138, 43, 226, 0.4);
  }
  50% { 
    transform: scale(1.05); 
    opacity: 1; 
    box-shadow: 0 8rpx 24rpx rgba(138, 43, 226, 0.6);
  }
}






/* 情绪墨迹区域 */
.emotion-ink {
  /* 升级墨迹区域背景效果 */
  background: linear-gradient(135deg, 
    rgba(255, 255, 255, 0.95) 0%, 
    rgba(255, 255, 255, 0.9) 100%);
  border-radius: 32rpx;
  padding: 30rpx;
  backdrop-filter: blur(25rpx);
  border: 2rpx solid rgba(255, 255, 255, 0.4);
  box-shadow: 
    0 20rpx 60rpx rgba(0, 0, 0, 0.15),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.8);
  position: relative;
  overflow: hidden;
}

/* 添加墨迹区域装饰效果 */
.emotion-ink::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4rpx;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(102, 126, 234, 0.5), 
    rgba(118, 75, 162, 0.5), 
    transparent);
  border-radius: 32rpx 32rpx 0 0;
}

.ink-title {
  text-align: center;
  margin-bottom: 40rpx;
}

.title-main {
  display: block;
  font-size: 36rpx;
  font-weight: 600;
  /* 升级标题渐变效果 */
  background: linear-gradient(135deg, #333 0%, #667eea 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 16rpx;
}

.title-sub {
  display: block;
  font-size: 26rpx;
  color: #666;
  line-height: 1.4;
}

.ink-canvas {
  width: calc(100% - 40rpx);
  height: 300rpx;
  /* 升级画布效果 */
  background: linear-gradient(135deg, #fafafa 0%, #f5f5f5 100%);
  border: 3rpx dashed rgba(102, 126, 234, 0.3);
  border-radius: 20rpx;
  margin: 0 20rpx 32rpx 20rpx;
  box-shadow: inset 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
  transition: all 0.3s ease;
}

.ink-canvas:active {
  border-color: rgba(102, 126, 234, 0.6);
  box-shadow: 
    inset 0 2rpx 8rpx rgba(0, 0, 0, 0.1),
    0 0 0 4rpx rgba(102, 126, 234, 0.1);
}

.ink-actions {
  display: flex;
  align-items: center;
  gap: 24rpx;
  justify-content: space-between;
  padding: 0 20rpx;
}

.main-action-area {
  flex: 1;
  display: flex;
  justify-content: center;
}

.ink-btn {
  padding: 24rpx 48rpx;
  border-radius: 48rpx;
  font-size: 28rpx;
  font-weight: 600;
  border: none;
  transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  position: relative;
}

.ink-btn.clear {
  /* 升级清空按钮效果 */
  background: linear-gradient(135deg, rgba(0, 0, 0, 0.08) 0%, rgba(0, 0, 0, 0.12) 100%);
  color: #666;
  border: 2rpx solid rgba(0, 0, 0, 0.1);
}

.ink-btn.clear:hover {
  background: linear-gradient(135deg, rgba(0, 0, 0, 0.12) 0%, rgba(0, 0, 0, 0.16) 100%);
  transform: translateY(-2rpx);
}

.ink-btn.generate {
  /* 升级生成按钮效果 */
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: #fff;
  box-shadow: 
    0 8rpx 24rpx rgba(102, 126, 234, 0.4),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.2);
  border: 2rpx solid rgba(255, 255, 255, 0.2);
}

.ink-btn.generate:hover {
  transform: translateY(-4rpx) scale(1.02);
  box-shadow: 
    0 12rpx 32rpx rgba(102, 126, 234, 0.5),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.3);
}

.ink-btn.loading {
  /* 升级加载状态效果 */
  background: linear-gradient(135deg, #ffa726 0%, #ff7043 100%);
  animation: generatePulse 2s ease-in-out infinite;
  box-shadow: 0 8rpx 24rpx rgba(255, 167, 38, 0.4);
}

@keyframes generatePulse {
  0%, 100% { 
    transform: scale(1); 
    box-shadow: 0 8rpx 24rpx rgba(255, 167, 38, 0.4);
  }
  50% { 
    transform: scale(1.05); 
    box-shadow: 0 12rpx 32rpx rgba(255, 167, 38, 0.6);
  }
}

/* 记忆回廊 - 修复与画布重叠问题 */
.memory-gallery {
  margin-top: 40rpx; /* 改为正值，增加与上方内容的间距 */
  margin-bottom: 60rpx;
  z-index: 1;
  position: relative;
}

.gallery-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 32rpx;
  padding: 0 16rpx;
}

.gallery-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #fff;
  /* 升级标题效果 */
  text-shadow: 
    0 2rpx 8rpx rgba(0, 0, 0, 0.3),
    0 0 20rpx rgba(255, 255, 255, 0.2);
  background: linear-gradient(135deg, #fff 0%, rgba(255, 255, 255, 0.8) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.gallery-count {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.9);
  /* 添加计数器装饰 */
  background: rgba(255, 255, 255, 0.15);
  padding: 6rpx 12rpx;
  border-radius: 15rpx;
  backdrop-filter: blur(10rpx);
}

.gallery-scroll {
  white-space: nowrap;
}

.memory-card {
  display: inline-block;
  width: 200rpx;
  height: 320rpx;
  margin-right: 24rpx;
  /* 升级记忆卡片效果 */
  background: linear-gradient(135deg, 
    rgba(255, 255, 255, 0.95) 0%, 
    rgba(255, 255, 255, 0.9) 100%);
  border-radius: 24rpx;
  backdrop-filter: blur(15rpx);
  border: 2rpx solid rgba(255, 255, 255, 0.4);
  box-shadow: 
    0 12rpx 32rpx rgba(0, 0, 0, 0.15),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.8);
  transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  position: relative;
  overflow: hidden;
}

.memory-card:hover {
  transform: translateY(-12rpx) scale(1.02);
  box-shadow: 
    0 20rpx 48rpx rgba(0, 0, 0, 0.25),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.9);
}

/* 背景图片 - 减弱模糊效果，提高可见度 */
.memory-bg {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 1;
  opacity: 0.8; /* 提高透明度，使背景图更清晰 */
  filter: blur(2rpx); /* 添加轻微模糊，避免过于突兀 */
}

/* 遮罩层 - 减弱遮罩效果，让背景图更明显 */
.memory-overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  /* 减弱遮罩强度，让背景图更明显 */
  background: linear-gradient(
    180deg,
    rgba(255, 255, 255, 0.7) 0%,
    rgba(255, 255, 255, 0.5) 30%,
    rgba(255, 255, 255, 0.4) 70%,
    rgba(255, 255, 255, 0.8) 100%
  );
  z-index: 2;
  backdrop-filter: blur(4rpx); /* 添加背景模糊保持层次感 */
}

.memory-content {
  position: relative;
  z-index: 3;
  padding: 24rpx;
  height: 100%;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

.memory-date {
  font-size: 18rpx;
  color: #666;
  margin-bottom: 12rpx;
  text-align: center;
}

.memory-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #333;
  text-align: center;
  margin-bottom: 16rpx;
  line-height: 1.3;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  /* 增强文字可读性 */
  text-shadow: 1rpx 1rpx 4rpx rgba(255, 255, 255, 0.8);
  background: rgba(255, 255, 255, 0.8);
  padding: 6rpx 12rpx;
  border-radius: 8rpx;
  margin: 0 auto 16rpx auto;
  max-width: 90%;
}

.memory-text {
  font-size: 20rpx;
  color: #555;
  line-height: 1.4;
  text-align: center;
  margin-bottom: 12rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  /* 增强文字可读性 */
  text-shadow: 1rpx 1rpx 2rpx rgba(255, 255, 255, 0.8);
}

.memory-mood-label {
  font-size: 18rpx;
  color: #8b5cf6;
  text-align: center;
  font-weight: 500;
  background: rgba(139, 92, 246, 0.2);
  padding: 6rpx 12rpx;
  border-radius: 12rpx;
  margin: 0 auto;
  display: inline-block;
  max-width: 120rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  /* 增强标签可读性 */
  backdrop-filter: blur(8rpx);
  box-shadow: 0 2rpx 8rpx rgba(139, 92, 246, 0.2);
}

.memory-mood-bar {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 8rpx;
  border-radius: 0 0 24rpx 24rpx;
  z-index: 4;
  /* 添加心情条发光效果 */
  box-shadow: 0 0 10rpx rgba(139, 92, 246, 0.5);
}

/* 空状态 */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 400rpx;
  text-align: center;
  z-index: 1;
  position: relative;
}

.empty-compass {
  margin-bottom: 40rpx;
}

.compass-ring.empty {
  border-color: rgba(255, 255, 255, 0.3);
  animation: none;
}

.compass-needle.inactive {
  background: rgba(255, 255, 255, 0.5);
  animation: none;
}

.empty-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #fff;
  margin-bottom: 16rpx;
  text-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.2);
}

.empty-subtitle {
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.8);
  line-height: 1.4;
}

/* 加载状态 */
.loading-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  /* 升级加载遮罩效果 */
  background: linear-gradient(135deg, 
    rgba(0, 0, 0, 0.8) 0%, 
    rgba(0, 0, 0, 0.6) 100%);
  backdrop-filter: blur(15rpx);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

.loading-compass .compass-ring {
  /* 升级加载罗盘效果 */
  border-color: rgba(255, 255, 255, 0.8);
  background: radial-gradient(circle, 
    rgba(255, 255, 255, 0.2) 0%, 
    rgba(255, 255, 255, 0.05) 100%);
  box-shadow: 
    0 0 30rpx rgba(255, 255, 255, 0.3),
    inset 0 0 20rpx rgba(255, 255, 255, 0.1);
}

.compass-needle.spinning {
  animation: spin 2s linear infinite;
  /* 添加旋转发光效果 */
  box-shadow: 0 0 15rpx rgba(255, 107, 107, 0.8);
}

@keyframes spin {
  from { 
    transform: rotate(0deg); 
    box-shadow: 0 0 15rpx rgba(255, 107, 107, 0.8);
  }
  to { 
    transform: rotate(360deg); 
    box-shadow: 0 0 20rpx rgba(255, 107, 107, 1);
  }
}

.loading-text {
  font-size: 32rpx;
  color: #fff;
  margin: 40rpx 0;
  /* 升级加载文字效果 */
  text-shadow: 
    0 2rpx 8rpx rgba(0, 0, 0, 0.5),
    0 0 20rpx rgba(255, 255, 255, 0.3);
  background: linear-gradient(135deg, #fff 0%, rgba(255, 255, 255, 0.8) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.loading-steps {
  display: flex;
  gap: 60rpx;
  margin-top: 40rpx;
}

.step {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
  opacity: 0.4;
  transition: all 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  /* 添加步骤背景装饰 */
  background: rgba(255, 255, 255, 0.05);
  padding: 20rpx;
  border-radius: 20rpx;
  backdrop-filter: blur(10rpx);
}

.step.active {
  opacity: 1;
  transform: scale(1.15);
  /* 升级激活状态效果 */
  background: rgba(255, 255, 255, 0.15);
  box-shadow: 0 8rpx 32rpx rgba(255, 255, 255, 0.2);
}

.step-icon {
  font-size: 40rpx;
  /* 添加图标发光效果 */
  filter: drop-shadow(0 0 10rpx rgba(255, 255, 255, 0.5));
}

.step-text {
  font-size: 22rpx;
  color: #fff;
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.3);
}

/* 环境状态显示 */
.date-info-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
}

.env-status {
  display: flex;
  align-items: center;
  gap: 8rpx;
  padding: 4rpx 12rpx;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 20rpx;
  backdrop-filter: blur(10rpx);
}

.status-dot {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
  background: #4ecdc4;
}

.status-dot.loading {
  animation: pulse 1.5s ease-in-out infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 0.4; transform: scale(1); }
  50% { opacity: 1; transform: scale(1.2); }
}

.status-text {
  font-size: 20rpx;
  color: rgba(255, 255, 255, 0.9);
}

/* 生成按钮等待状态 */
.generate.waiting {
  background: linear-gradient(135deg, #95a5a6, #7f8c8d);
  cursor: not-allowed;
}

.generate.waiting:hover {
  background: linear-gradient(135deg, #95a5a6, #7f8c8d);
  transform: none;
}

/* 响应式适配 */
@media screen and (max-width: 750rpx) {
  .compass-container {
    padding: 32rpx 24rpx;
  }
  
  .brand-title {
    font-size: 56rpx;
  }
  
  .loading-steps {
    gap: 40rpx;
  }
  
  .step {
    padding: 16rpx;
  }
}

/* 测试按钮样式 */
.test-button-container {
  text-align: center;
  margin: 40rpx 0;
}

.test-button {
  background: #ff6b6b;
  color: white;
  border-radius: 20rpx;
  padding: 20rpx 40rpx;
  font-size: 28rpx;
  border: none;
}

/* ==================== 美化卡片组件样式（仅用于降级显示） ==================== */
.beautiful-postcard {
  position: relative;
  width: 690rpx;
  height: 920rpx;
  margin: 0 auto;
  border-radius: 32rpx;
  overflow: hidden;
  box-shadow: 
    0 32rpx 100rpx rgba(0, 0, 0, 0.3),
    0 0 0 1rpx rgba(255, 255, 255, 0.1);
  background: #fff;
  transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

.beautiful-postcard:active {
  transform: scale(0.98);
  box-shadow: 
    0 16rpx 50rpx rgba(0, 0, 0, 0.4),
    0 0 0 1rpx rgba(255, 255, 255, 0.2);
}

.postcard-background {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 1;
}

.postcard-background .background-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.background-overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  /* 升级背景遮罩渐变 */
  background: linear-gradient(
    180deg,
    rgba(255, 255, 255, 0.98) 0%,
    rgba(255, 255, 255, 0.85) 30%,
    rgba(255, 255, 255, 0.75) 50%,
    rgba(255, 255, 255, 0.85) 70%,
    rgba(255, 255, 255, 0.98) 100%
  );
  backdrop-filter: blur(3rpx);
}

.postcard-content {
  position: relative;
  z-index: 2;
  height: 100%;
  padding: 70rpx 56rpx;
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
}

.postcard-header {
  text-align: center;
  margin-bottom: 28rpx;
}

.postcard-date {
  display: block;
  font-size: 26rpx;
  color: #8b5cf6;
  font-weight: 500;
  margin-bottom: 20rpx;
  letter-spacing: 2rpx;
}

.postcard-title {
  display: block;
  font-size: 54rpx;
  font-weight: bold;
  /* 升级标题渐变效果 */
  background: linear-gradient(135deg, #1a1a1a 0%, #667eea 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  line-height: 1.2;
  margin-bottom: 16rpx;
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.1);
}

.postcard-body {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  text-align: center;
  margin: 16rpx 0 24rpx 0;
}

.main-quote {
  margin-bottom: 40rpx;
}

.quote-text {
  display: block;
  font-size: 30rpx;
  line-height: 1.6;
  color: #2d3748;
  font-weight: 400;
  text-align: center;
  padding: 0 20rpx;
}

.english-quote {
  margin-top: 30rpx;
  padding: 30rpx 40rpx;
  background: linear-gradient(135deg, rgba(139, 92, 246, 0.1), rgba(99, 102, 241, 0.1));
  border-radius: 24rpx;
  border-left: 6rpx solid #8b5cf6;
}

.english-text {
  display: block;
  font-size: 24rpx;
  color: #6b7280;
  font-style: italic;
  line-height: 1.5;
  text-align: center;
  font-family: 'Georgia', serif;
}

.english-ribbon {
  margin-top: 24rpx;
  background: linear-gradient(135deg, rgba(99,102,241,.12), rgba(139,92,246,.12));
  border: 2rpx solid rgba(99,102,241,.25);
  border-radius: 20rpx;
  padding: 16rpx 24rpx;
}

.postcard-recommendations {
  margin: 30rpx 0;
  padding: 30rpx;
  /* 升级推荐区域效果 */
  background: linear-gradient(135deg, 
    rgba(99, 102, 241, 0.08) 0%, 
    rgba(139, 92, 246, 0.08) 100%);
  border-radius: 24rpx;
  border: 2rpx solid rgba(139, 92, 246, 0.15);
  box-shadow: 
    inset 0 1rpx 0 rgba(255, 255, 255, 0.8),
    0 4rpx 16rpx rgba(139, 92, 246, 0.1);
}

.recommendations-title {
  text-align: center;
  margin-bottom: 20rpx;
}

.rec-title-text {
  font-size: 24rpx;
  color: #8b5cf6;
  font-weight: 600;
  letter-spacing: 1rpx;
}

.postcard-recommendations .recommendation-item {
  display: flex;
  align-items: center;
  padding: 16rpx 0;
  border-bottom: 1rpx solid rgba(139, 92, 246, 0.1);
}

.postcard-recommendations .recommendation-item:last-child {
  border-bottom: none;
}

.postcard-recommendations .rec-icon {
  font-size: 28rpx;
  margin-right: 16rpx;
  width: 40rpx;
  text-align: center;
}

.postcard-recommendations .rec-content {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.rec-name {
  font-size: 26rpx;
  color: #374151;
  font-weight: 500;
  margin-bottom: 4rpx;
  line-height: 1.3;
}

.rec-author {
  font-size: 22rpx;
  color: #6b7280;
  font-weight: 300;
}

.postcard-footer {
  margin-top: auto;
  text-align: center;
}

.weather-info {
  margin-bottom: 24rpx;
}

.weather-text {
  font-size: 24rpx;
  color: #6b7280;
  background: rgba(229, 231, 235, 0.8);
  padding: 12rpx 24rpx;
  border-radius: 20rpx;
  display: inline-block;
}

.signature {
  margin-top: 20rpx;
}

.signature-text {
  font-size: 22rpx;
  color: #9ca3af;
  font-weight: 300;
  letter-spacing: 1rpx;
}

.ai-badge {
  position: absolute;
  top: 24rpx;
  right: 24rpx;
  z-index: 3;
  background: linear-gradient(135deg, #667eea, #764ba2);
  color: white;
  padding: 8rpx 16rpx;
  border-radius: 16rpx;
  box-shadow: 0 4rpx 12rpx rgba(102, 126, 234, 0.3);
}

.ai-text {
  font-size: 20rpx;
  font-weight: 500;
}

/* 卡片动画效果 */
.beautiful-postcard {
  animation: cardFadeIn 1s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  transition: transform 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

.beautiful-postcard:active {
  transform: scale(0.98);
}

@keyframes cardFadeIn {
  0% {
    opacity: 0;
    transform: translateY(60rpx) scale(0.9);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

/* ==================== 🔮 心境速测弹窗样式 ==================== */

/* 弹窗遮罩 */
.quiz-modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.6);
  backdrop-filter: blur(8rpx);
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 40rpx;
  box-sizing: border-box;
  animation: overlayFadeIn 0.3s ease-out;
}

@keyframes overlayFadeIn {
  0% {
    opacity: 0;
  }
  100% {
    opacity: 1;
  }
}

/* 弹窗主体 */
.quiz-modal {
  width: 100%;
  max-width: 640rpx;
  min-height: 500rpx;
  background: linear-gradient(135deg, #f8fafc 0%, #ffffff 100%);
  border-radius: 32rpx;
  box-shadow: 
    0 24rpx 64rpx rgba(0, 0, 0, 0.15),
    0 8rpx 32rpx rgba(0, 0, 0, 0.1);
  overflow: hidden;
  animation: modalSlideIn 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  display: flex;
  flex-direction: column;
}

@keyframes modalSlideIn {
  0% {
    opacity: 0;
    transform: translateY(60rpx) scale(0.95);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

/* 弹窗头部 */
.quiz-header {
  position: relative;
  padding: 32rpx 40rpx 24rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
}

.quiz-title {
  font-size: 36rpx;
  font-weight: 600;
  text-align: center;
  margin-bottom: 20rpx;
  letter-spacing: 1rpx;
}

.quiz-progress {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.progress-text {
  font-size: 24rpx;
  font-weight: 500;
  opacity: 0.9;
  min-width: 80rpx;
}

.progress-bar {
  flex: 1;
  height: 8rpx;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 4rpx;
  overflow: hidden;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #ffffff 0%, #f0f9ff 100%);
  border-radius: 4rpx;
  transition: width 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  box-shadow: 0 0 8rpx rgba(255, 255, 255, 0.3);
}

.quiz-close {
  position: absolute;
  top: 24rpx;
  right: 24rpx;
  width: 60rpx;
  height: 60rpx;
  background: rgba(255, 255, 255, 0.1);
  border: none;
  border-radius: 50%;
  color: white;
  font-size: 32rpx;
  line-height: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.2s ease;
}

.quiz-close:active {
  background: rgba(255, 255, 255, 0.2);
  transform: scale(0.95);
}

/* 问题内容 */
.quiz-content {
  flex: 1;
  padding: 40rpx 40rpx 32rpx;
  display: flex;
  flex-direction: column;
}

.question-container {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.question-text {
  font-size: 32rpx;
  font-weight: 500;
  color: #1f2937;
  line-height: 1.5;
  margin-bottom: 40rpx;
  text-align: center;
  letter-spacing: 0.5rpx;
}

.options-container {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.option-item {
  padding: 24rpx 32rpx;
  background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
  border: 2rpx solid #e2e8f0;
  border-radius: 24rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  cursor: pointer;
}

.option-item:hover {
  border-color: #667eea;
  background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%);
  transform: translateY(-2rpx);
  box-shadow: 0 8rpx 24rpx rgba(102, 126, 234, 0.15);
}

.option-item:active {
  transform: translateY(0) scale(0.98);
  box-shadow: 0 4rpx 12rpx rgba(102, 126, 234, 0.2);
}

.option-label {
  font-size: 28rpx;
  color: #374151;
  font-weight: 400;
  line-height: 1.4;
  flex: 1;
}

.option-icon {
  font-size: 28rpx;
  color: #667eea;
  font-weight: 600;
  margin-left: 16rpx;
  transition: transform 0.2s ease;
}

.option-item:hover .option-icon {
  transform: translateX(4rpx);
}

/* 完成状态 */
.quiz-complete {
  padding: 60rpx 40rpx;
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20rpx;
}

.complete-icon {
  font-size: 80rpx;
  margin-bottom: 12rpx;
  animation: completeIconBounce 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

@keyframes completeIconBounce {
  0% {
    opacity: 0;
    transform: scale(0.3);
  }
  50% {
    transform: scale(1.1);
  }
  100% {
    opacity: 1;
    transform: scale(1);
  }
}

.complete-text {
  font-size: 36rpx;
  font-weight: 600;
  color: #1f2937;
  margin-bottom: 8rpx;
}

.complete-subtitle {
  font-size: 26rpx;
  color: #6b7280;
  font-weight: 400;
  line-height: 1.4;
}

/* 底部操作 */
.quiz-footer {
  padding: 24rpx 40rpx 40rpx;
  display: flex;
  justify-content: center;
}

.quiz-skip-btn {
  padding: 20rpx 40rpx;
  background: transparent;
  border: 2rpx solid #d1d5db;
  border-radius: 24rpx;
  color: #6b7280;
  font-size: 26rpx;
  font-weight: 500;
  transition: all 0.2s ease;
}

.quiz-skip-btn:active {
  border-color: #9ca3af;
  color: #4b5563;
  transform: scale(0.98);
}

/* 新主按钮样式 - 心境速测 */
.ink-btn.quiz-primary {
  min-width: 200rpx;
  max-width: 400rpx;
  padding: 24rpx 48rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border: none;
  font-weight: 600;
  font-size: 28rpx;
  transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  box-shadow: 
    0 8rpx 24rpx rgba(102, 126, 234, 0.4),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.2);
  position: relative;
  white-space: nowrap;
  overflow: visible;
  text-overflow: clip;
}

.ink-btn.quiz-primary:active {
  transform: translateY(-2rpx) scale(1.02);
  box-shadow: 
    0 12rpx 32rpx rgba(102, 126, 234, 0.5),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.3);
}

/* 新主按钮样式 - 生成心象签 */
.ink-btn.generate-primary {
  min-width: 200rpx;
  max-width: 400rpx;
  padding: 24rpx 48rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border: none;
  font-weight: 600;
  font-size: 28rpx;
  transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  box-shadow: 
    0 8rpx 24rpx rgba(102, 126, 234, 0.4),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.2);
  position: relative;
  white-space: nowrap;
  overflow: visible;
  text-overflow: clip;
}

.ink-btn.generate-primary:active {
  transform: translateY(-2rpx) scale(1.02);
  box-shadow: 
    0 12rpx 32rpx rgba(102, 126, 234, 0.5),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.3);
}

.ink-btn.generate-primary.loading {
  background: linear-gradient(135deg, #ffa726 0%, #ff7043 100%);
  animation: generatePulse 2s ease-in-out infinite;
  box-shadow: 0 8rpx 24rpx rgba(255, 167, 38, 0.4);
}

.ink-btn.generate-primary.waiting {
  background: linear-gradient(135deg, #95a5a6, #7f8c8d);
  cursor: not-allowed;
}

/* 保留原有quiz按钮样式用于其他地方 */
.ink-btn.quiz {
  background: linear-gradient(135deg, #8b5cf6 0%, #a855f7 100%);
  color: white;
  border: none;
  font-weight: 500;
  transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  box-shadow: 0 4rpx 16rpx rgba(139, 92, 246, 0.3);
}

.ink-btn.quiz:active {
  transform: translateY(2rpx) scale(0.98);
  box-shadow: 0 2rpx 8rpx rgba(139, 92, 246, 0.4);
}

/* ==================== 🔮 挂件式心象签优化样式 ==================== */

/* 🔮 心象签主显示区域 - 简化结构，最大化挂件空间 */
.charm-main-area {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  min-height: 75vh;  /* 增大到75%视窗高度 */
  padding: 10rpx;
  margin: 0 auto 40rpx auto; /* 增加下边距，避免与回廊重叠 */
  box-sizing: border-box;
  position: relative;
  z-index: 10;
}

/* 挂件组件样式优化 */
.charm-main-area hanging-charm {
  filter: drop-shadow(0 20rpx 40rpx rgba(0, 0, 0, 0.15));
  transition: transform 0.3s ease;
  z-index: 11;
}

/* 降级显示区域 */
.fallback-card-area {
  width: 90%;
  max-width: 600rpx;
  margin: 40rpx auto;
  border-radius: 24rpx;
  overflow: hidden;
  box-shadow: 0 20rpx 40rpx rgba(0, 0, 0, 0.1);
}

/* 🔮 挂件模式下的空间优化 */
.compass-container.charm-mode {
  padding-top: 20rpx;  /* 减少顶部padding */
  padding-bottom: calc(120rpx + env(safe-area-inset-bottom)); /* 进一步减少底部padding */
}

.compass-container.charm-mode .user-status {
  margin-bottom: 20rpx;  /* 进一步减少间距 */
}

